setup:
prereq: need node.js
(sometimes it doesn't work in vscode due to user authorization scopes, so run the following commands in command prompt)

run npm install for dependencies
run npm run dev to run the app --> access it at http://localhost:5173